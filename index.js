module.exports = {
	// chain prototypes
	chain: require('./chain'),
	// merge mixins
	merge: require('./merge'),
	// merge props (in general with defaults)
	props: require('./props'),
	ensure: require('./props')
};
